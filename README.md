# 🦸‍♂️ Superhero App

Fullstack-додаток для візуалізації та керування базою даних супергероїв. Проєкт реалізований як технічне завдання, що демонструє навички роботи з React, Redux, Node.js та хмарними сервісами зберігання зображень.

**⚠️ Примітка:** Оскільки бекенд розгорнутий на безкоштовному тарифі Render, при першому зверненні до сайту (після тривалої перерви) серверу може знадобитися **30-60 секунд для "розігріву"** (холодний старт). Будь ласка, дочекайтеся завантаження.

## 🔗 Live Links

- **Frontend (Demo):** [Link](https://superhero-app-five.vercel.app/)
- **Backend (API):** [Link](https://superhero-app-0he6.onrender.com/heroes)

---

## 🚀 Основний функціонал

- **Каталог героїв:** Перегляд списку героїв з пагінацією (по 5 карток на сторінці).
- **Детальна сторінка:** Повна інформація про героя з галереєю зображень.
- **Створення (Create):** Додавання нового героя з можливістю завантаження кількох фото.
- **Редагування (Update):** Зручна форма для зміни текстових даних та управління фотографіями (видалення старих або додавання нових).
- **Видалення (Delete):** Повне видалення героя та його даних з бази.

---

## 🛠 Технологічний стек

### Frontend

- **React** — бібліотека для інтерфейсу.
- **Redux Toolkit** — управління глобальним станом (слайси, асинхронні thunks).
- **React Router DOM** — навігація між сторінками.
- **Axios** — взаємодія з API.
- **Styled Components** — стилізація компонентів.
- **React-icons** — набір іконок для інтерфейсу.
- **React-spinners** — індикатори завантаження для кращого UX.

### Backend

- **Node.js & Express** — серверна частина.
- **MongoDB & Mongoose** — база даних та моделювання даних.
- **Multer & Cloudinary** — обробка та зберігання медіа-файлів.
- **Joi** — сувора валідація вхідних даних на рівні сервера.

---

## 📡 API Reference

Всі запити до API використовують заголовок `Content-Type: multipart/form-data` при передачі файлів.

| Метод    | Ендпоінт      | Опис                               |
| :------- | :------------ | :--------------------------------- |
| `GET`    | `/heroes`     | Отримати список героїв (пагінація) |
| `GET`    | `/heroes/:id` | Детальна інформація про героя      |
| `POST`   | `/heroes`     | Створити нового героя              |
| `PUT`    | `/heroes/:id` | Оновити дані героя                 |
| `DELETE` | `/heroes/:id` | Видалити героя                     |

---

## 🏗 Як встановити проект локально

1.  **Клонуйте репозиторій:**

    ```bash
    git clone https://github.com/Pozniak17/superhero-app.git
    ```

2.  **Встановіть залежності (для обох частин):**

    ```bash
    # Для бекенду
    cd server && npm install
    # Для фронтенду
    cd client && npm install
    ```

3.  **Налаштуйте змінні оточення (.env):**
    Створіть `.env` файл у папці сервера:

    ```env
    PORT=3000
    DB_HOST=your_mongodb_connection_string
    CLOUDINARY_NAME=your_name
    CLOUDINARY_KEY=your_key
    CLOUDINARY_SECRET=your_secret
    ```

4.  **Запустіть проект:**
    ```bash
    npm run dev
    ```

---

## 📋 Валідація даних (Joi Schema)

Кожен об'єкт героя проходить перевірку на відповідність схемі:

- `nickname`: Обов'язкове, рядок.
- `real_name`: Обов'язкове, рядок.
- `origin_description`: Обов'язкове, довгий опис.
- `superpowers`: Обов'язкове, перелік здібностей.
- `catch_phrase`: Обов'язкове, фраза героя.
- `images`: Масив файлів або посилань.

---

## 🤝 Контакти

**Автор:** [Pozniak17](https://github.com/Pozniak17)  
**Email:** pozniak.yevhenii@gmail.com
